import 'package:flutter/material.dart';

class AddClothesView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Add Clothes Screen'),
    );
  }
}
